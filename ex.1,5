void main() {
  isAuthorized('duda', idade: 78);
}

void isAuthorized(String name, {int? idade}) {
  List<String> auth = ['duda', 'suzana', 'geovana'];
  if (auth.contains(name) && idade! >= 18) {
    print('autorizado');
  } else {
    print('não autorizado');
  }
}
